import { CloseAfterDirective } from './close-after.directive';

describe('CloseAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseAfterDirective();
    expect(directive).toBeTruthy();
  });
});
